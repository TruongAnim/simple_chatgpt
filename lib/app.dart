import 'package:flutter/material.dart';

class ChatGptApp extends MaterialApp {
  const ChatGptApp({super.key})
      : super(
            home: const Scaffold(
          body: Text('hello'),
        ));
}
